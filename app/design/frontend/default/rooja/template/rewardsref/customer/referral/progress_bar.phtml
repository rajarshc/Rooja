
    <?php $_referred = $this->getReferred();?>
   
<?php if($this->showHistory()): ?>
<br/><br/>    
<h3 class="page-invite-head fl">Track Your Progress</h3>

	<br/>
	<?php 
		$referrerCount = count($_referred);
		$referrerPercent = $referrerCount * 10;
		$awayFrom10 = 10 - $referrerCount;
	?>
	<h3 class="fr" id="howClose"><?php if($referrerCount == 10){ echo "Congratulations. Enjoy Free Shipping for 2 months"; }else{ ?>Invite <?php echo $awayFrom10 ?> more friends to get Free Standard Shipping for 2 months.<?php } ?><br>Bonus: Rs. 1000 credit when your friend makes first Rs.1000 purchase within 30 days of joining.</h3>
	<span id="referralBar" class="clear"><span id="numReferrals" style="width:<?php echo $referrerPercent; ?>%;"></span></span>
	
	<?php
		if($referrerCount>0){
			$customer = Mage::getModel('customer/customer')->load($customerId);
			if ($customer->getId()) {
			    $customer->setData('group_id', 5);
			    $customer->save(); 
			}
		}
	?>
  
<?php endif ?>